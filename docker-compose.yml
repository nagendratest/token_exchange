services:
  token-generator-service-test:
    profiles:
      - test
    build:
      context: ./token-generator-service
    environment:
      SPRING_PROFILES_ACTIVE: test
    ports:
      - "8081:8081"
  token-generator-service-prod:
    profiles:
      - prod
    build:
      context: ./token-generator-service
    environment:
      SPRING_PROFILES_ACTIVE: prod
    ports:
      - "8081:8081"
