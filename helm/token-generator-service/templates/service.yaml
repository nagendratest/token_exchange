apiVersion: v1
kind: Service
metadata:
  name: {{ include "token-generator-service.fullname" . }}
  labels:
    {{- include "token-generator-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "token-generator-service.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
